# Netlify serves the existing FLYPY MONSTER UI.
# After the Node backend is deployed, replace YOUR-RENDER-URL below
# with the exact HTTPS URL of that backend.

[[redirects]]
  from = "/api/*"
  to = "https://crimsonmeadow-27286.app.onebit.ng/api/:splat"
  status = 200
  force = true
